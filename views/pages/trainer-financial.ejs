<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <title>Financeiro | Momentum Fit</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <div class="dashboard-container">
        <%- include('../partials/sidebar') %>
        <main class="dashboard-main">
            <div class="page-header" style="text-align: left; background: transparent; padding-left: 0;">
                <h1>Carteira Digital</h1>
            </div>

            <div class="card-box" style="background: linear-gradient(135deg, #222, #111); margin-bottom: 30px; display: flex; justify-content: space-between; align-items: center; padding: 40px;">
                <div>
                    <span style="color: #888; font-size: 0.9rem;">Saldo Disponível</span>
                    <h2 style="font-size: 3rem; color: var(--primary-color); margin: 5px 0;">R$ <%= typeof balance !== 'undefined' ? balance.toFixed(2) : '0.00' %></h2>
                </div>
                <button class="btn btn-primary btn-lg">Solicitar Saque</button>
            </div>

            <h3 style="margin-bottom: 20px;">Histórico de Transações</h3>
            <div class="card-box" style="padding: 0;">
                <table style="width: 100%; border-collapse: collapse; color: white;">
                    <thead>
                        <tr style="border-bottom: 1px solid #333; text-align: left;">
                            <th style="padding: 15px;">Data</th>
                            <th style="padding: 15px;">Descrição</th>
                            <th style="padding: 15px;">Tipo</th>
                            <th style="padding: 15px; text-align: right;">Valor</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% if (transactions && transactions.length > 0) { %>
                            <% transactions.forEach(t => { %>
                                <tr style="border-bottom: 1px solid #222;">
                                    <td style="padding: 15px; color: #666;"><%= new Date(t.created_at).toLocaleDateString() %></td>
                                    <td style="padding: 15px;"><%= t.description %></td>
                                    <td style="padding: 15px;">
                                        <span style="padding: 4px 10px; border-radius: 4px; font-size: 0.8rem; background: <%= t.type === 'payout' ? 'rgba(255,0,0,0.1)' : 'rgba(0,255,0,0.1)' %>; color: <%= t.type === 'payout' ? '#ff6b6b' : '#bef202' %>;">
                                            <%= t.type %>
                                        </span>
                                    </td>
                                    <td style="padding: 15px; text-align: right; font-weight: bold; color: <%= t.type === 'payout' ? '#ff6b6b' : 'white' %>;">
                                        <%= t.type === 'payout' ? '-' : '+' %> R$ <%= t.amount %>
                                    </td>
                                </tr>
                            <% }) %>
                        <% } else { %>
                            <tr><td colspan="4" style="padding: 20px; text-align: center; color: #666;">Nenhuma transação encontrada.</td></tr>
                        <% } %>
                    </tbody>
                </table>
            </div>
        </main>
    </div>
    <script src="/js/main.js"></script>
</body>
</html>
