<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <%- include('../layouts/app/head') %>
    <title>Evolução | Momentum Fit</title>
</head>
<body>
    <div class="dashboard-wrapper">
        <%- include('../partials/sidebar') %>
        <div class="main-content">
            <%- include('../partials/app-header', { title: 'Métricas de Performance' }) %>

            <div class="content-pad">
                <div class="grid-2">
                    <div class="card">
                        <div class="card-header">
                            <h3 class="card-title">Peso Corporal (kg)</h3>
                        </div>
                        <div style="height: 200px; display: flex; align-items: flex-end; justify-content: space-around; padding-bottom: 10px; border-bottom: 1px solid #333;">
                            <% data.weight.forEach(val => { %>
                                <div style="width: 40px; background: var(--neon-primary); height: <%= val %>px; border-radius: 4px 4px 0 0; position: relative;">
                                    <span style="position: absolute; top: -20px; left: 5px; font-size: 0.7rem; color: white;"><%= val %></span>
                                </div>
                            <% }) %>
                        </div>
                        <div style="display: flex; justify-content: space-around; margin-top: 10px; color: #888; font-size: 0.8rem;">
                            <% data.labels.forEach(l => { %> <span><%= l %></span> <% }) %>
                        </div>
                    </div>

                    <div class="card">
                        <div class="card-header">
                            <h3 class="card-title">1RM Supino (Estimado)</h3>
                        </div>
                        <div style="height: 200px; display: flex; align-items: flex-end; justify-content: space-around; padding-bottom: 10px; border-bottom: 1px solid #333;">
                            <% data.benchPress.forEach(val => { %>
                                <div style="width: 40px; background: #3b82f6; height: <%= val * 1.5 %>px; border-radius: 4px 4px 0 0; position: relative;">
                                    <span style="position: absolute; top: -20px; left: 5px; font-size: 0.7rem; color: white;"><%= val %></span>
                                </div>
                            <% }) %>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
