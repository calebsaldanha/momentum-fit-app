<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <%- include('../partials/header') %>
    <link rel="stylesheet" href="/css/dashboard.css">
    <style>
        .details-grid {
            display: grid;
            grid-template-columns: 350px 1fr;
            gap: 2rem;
            margin-top: 2rem;
        }
        @media (max-width: 900px) { .details-grid { grid-template-columns: 1fr; } }
        
        .profile-hero {
            text-align: center;
            padding: 2rem;
            background: linear-gradient(to bottom, rgba(var(--primary-rgb), 0.1), transparent);
            border-radius: 16px 16px 0 0;
            margin: -1.5rem -1.5rem 1.5rem -1.5rem;
        }
        .profile-avatar-xl {
            width: 140px;
            height: 140px;
            border-radius: 50%;
            object-fit: cover;
            border: 4px solid #1a1a1a;
            box-shadow: 0 10px 30px rgba(0,0,0,0.5);
            margin-bottom: 1rem;
        }
        
        .info-list { list-style: none; padding: 0; margin: 0; }
        .info-list li {
            display: flex;
            justify-content: space-between;
            padding: 1rem 0;
            border-bottom: 1px solid rgba(255,255,255,0.05);
            color: #ccc;
        }
        .info-list li span:first-child { color: #888; }
        
        .client-card-mini {
            display: flex;
            align-items: center;
            gap: 1rem;
            padding: 1rem;
            background: rgba(255,255,255,0.02);
            border-radius: 8px;
            margin-bottom: 0.5rem;
            border: 1px solid transparent;
            transition: all 0.2s;
        }
        .client-card-mini:hover {
            background: rgba(255,255,255,0.05);
            border-color: rgba(255,255,255,0.1);
        }
        .avatar-xs { width: 32px; height: 32px; border-radius: 50%; object-fit: cover; }

        /* Inputs estilizados (não brancos quadrados) */
        .form-control-dark {
            background: #121212;
            border: 1px solid #333;
            color: #fff;
            padding: 0.8rem 1rem;
            border-radius: 8px;
            width: 100%;
        }
        .form-control-dark:focus {
            border-color: var(--primary-color);
            outline: none;
            box-shadow: 0 0 0 2px rgba(var(--primary-rgb), 0.2);
        }
    </style>
</head>
<body>
    <div class="dashboard-container">
        <%- include('../partials/sidebar', { user: user, currentPage: 'superadmin-manage' }) %>

        <main class="dashboard-main">
            <header class="dashboard-header">
                <div class="header-welcome">
                    <h1>Perfil do Profissional</h1>
                    <p class="text-muted">Gerenciamento e métricas de desempenho.</p>
                </div>
                <div class="header-actions">
                    <a href="/superadmin/manage" class="btn-secondary">
                        <i class="fas fa-arrow-left"></i> Voltar
                    </a>
                </div>
            </header>

            <div class="details-grid">
                <aside>
                    <div class="dashboard-card p-0 overflow-hidden">
                        <div class="profile-hero">
                            <img src="<%= trainer.profile_image || '/images/default-profile.png' %>" class="profile-avatar-xl">
                            <h2 class="mb-1"><%= trainer.name %></h2>
                            <span class="badge badge-primary">Treinador</span>
                            <span class="badge badge-<%= trainer.status === 'active' ? 'success' : 'danger' %>">
                                <%= trainer.status === 'active' ? 'Ativo' : 'Inativo' %>
                            </span>
                        </div>
                        
                        <div class="p-4">
                            <h4 class="mb-3">Informações</h4>
                            <ul class="info-list">
                                <li>
                                    <span>Email</span>
                                    <span><%= trainer.email %></span>
                                </li>
                                <li>
                                    <span>Cadastrado em</span>
                                    <span><%= new Date(trainer.created_at).toLocaleDateString() %></span>
                                </li>
                                <li>
                                    <span>Total Alunos</span>
                                    <strong class="text-primary"><%= clients.length %></strong>
                                </li>
                            </ul>

                            <hr class="my-4 border-secondary">

                            <h4 class="mb-3">Segurança e Acesso</h4>
                            <form action="/superadmin/users/<%= trainer.id %>/change-password" method="POST" class="mb-3">
                                <div class="form-group mb-2">
                                    <label class="small text-muted mb-1">Redefinir Senha</label>
                                    <div class="d-flex gap-2">
                                        <input type="password" name="new_password" class="form-control-dark" placeholder="Nova senha..." required>
                                        <button type="submit" class="btn-primary btn-sm"><i class="fas fa-key"></i></button>
                                    </div>
                                </div>
                            </form>

                            <form action="/superadmin/users/<%= trainer.id %>/status" method="POST" class="mb-2">
                                <input type="hidden" name="status" value="<%= trainer.status === 'active' ? 'inactive' : 'active' %>">
                                <button class="btn-secondary w-100">
                                    <%= trainer.status === 'active' ? 'Bloquear Acesso' : 'Desbloquear' %>
                                </button>
                            </form>
                            
                            <form action="/superadmin/users/<%= trainer.id %>/delete" method="POST" onsubmit="return confirm('ATENÇÃO: Isso removerá o treinador e deixará seus alunos órfãos. Continuar?');">
                                <button class="btn-danger w-100">Excluir Conta</button>
                            </form>
                        </div>
                    </div>
                </aside>

                <div class="content-col">
                    <div class="dashboard-card">
                        <div class="d-flex justify-content-between align-items-center mb-4">
                            <h3><i class="fas fa-users"></i> Carteira de Alunos</h3>
                            <span class="badge badge-secondary"><%= clients.length %> alunos</span>
                        </div>

                        <% if (clients.length > 0) { %>
                            <div class="clients-list">
                                <% clients.forEach(client => { %>
                                    <div class="client-card-mini">
                                        <img src="<%= client.profile_image || '/images/default-profile.png' %>" class="avatar-xs">
                                        <div style="flex: 1;">
                                            <div class="fw-bold"><%= client.name %></div>
                                            <small class="text-muted"><%= client.email %></small>
                                        </div>
                                        <a href="/superadmin/users/<%= client.id || client.client_real_id %>" class="btn-outline btn-sm">
                                            Gerenciar
                                        </a>
                                    </div>
                                <% }) %>
                            </div>
                        <% } else { %>
                            <div class="text-center py-5 text-muted">
                                <i class="fas fa-user-slash fa-3x mb-3 opacity-25"></i>
                                <p>Este treinador ainda não possui alunos vinculados.</p>
                                <p class="small">Vá em "Gerenciar Usuários" para atribuir alunos.</p>
                            </div>
                        <% } %>
                    </div>
                </div>
            </div>
        </main>
    </div>
</body>
</html>
