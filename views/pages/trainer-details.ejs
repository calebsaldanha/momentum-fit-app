<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <%- include('../partials/header') %>
    <link rel="stylesheet" href="/css/dashboard.css">
</head>
<body>
    <div class="dashboard-container">
        <%- include('../partials/sidebar', { user: user, currentPage: 'superadmin-manage' }) %>

        <main class="dashboard-main">
            <header class="d-flex justify-content-between align-items-center mb-4">
                <h1>Perfil do Treinador</h1>
                <a href="/superadmin/manage" class="btn btn-secondary"><i class="fas fa-arrow-left"></i> Voltar</a>
            </header>

            <div style="display: grid; grid-template-columns: 350px 1fr; gap: 2rem;">
                <div class="dashboard-card text-center">
                    <img src="<%= trainer.profile_image || '/images/default-profile.png' %>" style="width:120px; height:120px; border-radius:50%; object-fit:cover; border: 4px solid var(--primary-color); margin-bottom:1rem;">
                    <h2><%= trainer.name %></h2>
                    <p class="text-muted"><%= trainer.email %></p>
                    <div class="mt-3">
                        <span class="badge badge-<%= trainer.status %>"><%= trainer.status === 'active' ? 'Ativo' : 'Inativo' %></span>
                    </div>

                    <hr style="border-color: var(--border-color); margin: 1.5rem 0;">

                    <h4 class="text-start mb-3">Ações</h4>
                    <form action="/superadmin/users/<%= trainer.id %>/status" method="POST" class="mb-2">
                        <input type="hidden" name="status" value="<%= trainer.status === 'active' ? 'inactive' : 'active' %>">
                        <button class="btn btn-secondary w-100"><%= trainer.status === 'active' ? 'Bloquear Acesso' : 'Desbloquear' %></button>
                    </form>
                    
                    <form action="/superadmin/users/<%= trainer.id %>/delete" method="POST" onsubmit="return confirm('ATENÇÃO: Excluir este treinador removerá o vínculo com seus alunos. Confirmar?');">
                        <button class="btn btn-danger w-100">Excluir Conta</button>
                    </form>
                </div>

                <div class="dashboard-card">
                    <div class="d-flex justify-content-between mb-3">
                        <h3>Carteira de Alunos</h3>
                        <span class="badge badge-secondary"><%= clients.length %> alunos</span>
                    </div>

                    <% if (clients.length > 0) { %>
                        <div class="table-responsive">
                            <table class="modern-table">
                                <tbody>
                                    <% clients.forEach(c => { %>
                                        <tr>
                                            <td>
                                                <div class="user-info-flex">
                                                    <img src="<%= c.profile_image || '/images/default-profile.png' %>" class="user-avatar-sm">
                                                    <div><%= c.name %></div>
                                                </div>
                                            </td>
                                            <td class="text-end">
                                                <a href="/superadmin/users/<%= c.id || c.client_real_id %>" class="btn btn-sm btn-secondary">Gerenciar</a>
                                            </td>
                                        </tr>
                                    <% }) %>
                                </tbody>
                            </table>
                        </div>
                    <% } else { %>
                        <div class="text-center py-5 text-muted">
                            <i class="fas fa-user-slash fa-3x mb-3 opacity-25"></i>
                            <p>Este treinador não possui alunos vinculados.</p>
                        </div>
                    <% } %>
                </div>
            </div>
        </main>
    </div>
</body>
</html>
