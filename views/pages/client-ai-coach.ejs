<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <%- include('../layouts/app/head') %>
    <title><%= title %> | Momentum Fit</title>
</head>
<body class="bg-gray-50">
    <div class="flex h-screen overflow-hidden">
        <%- include('../partials/sidebar') %>
        <div class="relative flex flex-col flex-1 overflow-hidden"> <%- include('../partials/app-header') %>
            
            <main class="flex-grow flex flex-col h-full overflow-hidden bg-white relative">
                <div id="chat-container" class="flex-1 overflow-y-auto p-4 space-y-4 bg-gray-50">
                    <div class="flex items-start">
                        <div class="bg-blue-100 p-3 rounded-lg rounded-tl-none max-w-[80%] text-sm text-blue-900 shadow-sm">
                            <p class="font-bold text-xs mb-1">Momentum IA</p>
                            Olá, <%= user.name.split(' ')[0] %>! Sou seu coach virtual. Pode me perguntar sobre execução de exercícios, dieta ou tirar dúvidas sobre seu treino.
                        </div>
                    </div>
                </div>

                <div class="p-4 bg-white border-t border-gray-200">
                    <form class="flex gap-2" onsubmit="event.preventDefault(); alert('Integração com IA será ativada no próximo passo!');">
                        <input type="text" placeholder="Digite sua dúvida..." class="flex-1 border border-gray-300 rounded-full px-4 py-2 focus:ring-2 focus:ring-blue-500 outline-none">
                        <button type="submit" class="bg-blue-600 text-white w-10 h-10 rounded-full flex items-center justify-center hover:bg-blue-700 transition shadow-md">
                            <i class="fas fa-paper-plane"></i>
                        </button>
                    </form>
                </div>
            </main>
        </div>
    </div>
</body>
</html>
