<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cadastro - Momentum Fit</title>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="/css/styles.css">
    <style>
        /* Estilos Específicos de Auth para garantir o visual imersivo */
        .auth-body { 
            background: url('/images/auth-bg.png') no-repeat center center/cover;
            min-height: 100vh; 
            display: flex; 
            flex-direction: column;
            position: relative;
            padding-top: 70px; /* Espaço para o header fixo */
        }
        .auth-overlay { 
            position: absolute;
            inset: 0; 
            background: rgba(0,0,0,0.7); 
            z-index: 1; 
        }
        
        .auth-wrapper {
            flex: 1;
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
            z-index: 2;
            padding: 2rem 1rem;
        }

        .auth-card { 
            background: rgba(20, 20, 20, 0.95);
            padding: 2.5rem; 
            border-radius: 12px; 
            border: 1px solid rgba(255,255,255,0.1); 
            width: 100%; 
            max-width: 450px; 
            text-align: center; 
            box-shadow: 0 20px 50px rgba(0,0,0,0.5);
            backdrop-filter: blur(5px);
        }
        
        .form-group { margin-bottom: 1.2rem; text-align: left; }
        .form-group label { display: block; font-size: 0.85rem; color: #ccc; margin-bottom: 5px; font-weight: 600; }
        .form-group input, .form-group select { 
            width: 100%;
            padding: 12px; 
            background: #0a0a0a; 
            border: 1px solid #333; 
            color: #fff; 
            border-radius: 6px; 
            outline: none; 
            transition: 0.3s;
            font-family: 'Montserrat', sans-serif;
        }
        .form-group input:focus, .form-group select:focus { border-color: var(--primary-color); }
        
        .btn-auth {
            width: 100%;
            padding: 14px;
            font-weight: 800;
            background-color: var(--primary-color);
            color: #000;
            border: none;
            border-radius: 6px;
            cursor: pointer;
            transition: 0.3s;
            font-size: 1rem;
            text-transform: uppercase;
        }
        .btn-auth:hover { background-color: var(--primary-hover); transform: translateY(-2px); }

        /* Ajuste do Header transparente para essa página */
        .auth-header-nav {
            position: fixed;
            top: 0; left: 0; right: 0;
            z-index: 1000;
            background: rgba(0,0,0,0.8);
            backdrop-filter: blur(10px);
            border-bottom: 1px solid rgba(255,255,255,0.1);
            height: 70px;
            display: flex;
            align-items: center;
            padding: 0 20px;
        }
        .header-content {
            width: 100%; max-width: 1200px; margin: 0 auto;
            display: flex; justify-content: space-between; align-items: center;
        }
        .nav-links a { color: #ccc; margin-left: 20px; font-size: 0.9rem; font-weight: 600; }
        .nav-links a:hover { color: var(--primary-color); }
        
        @media(max-width: 768px) {
            .nav-links { display: none; } /* Simplifica no mobile do auth */
        }
    </style>
</head>
<body class="auth-body">
    
    <header class="auth-header-nav">
        <div class="header-content">
            <a href="/" class="brand-logo" style="color:white; font-weight:800; font-size:1.2rem; display:flex; align-items:center; gap:10px;">
                <img src="/images/momentum-fit-icon.png" alt="Logo" style="height:30px;">
                MOMENTUM FIT
            </a>
            <nav class="nav-links">
                <a href="/">Início</a>
                <a href="/about">Sobre</a>
                <a href="/articles">Artigos</a>
                <a href="/auth/login" style="color:var(--primary-color);">Entrar</a>
            </nav>
        </div>
    </header>

    <div class="auth-overlay"></div>
    
    <div class="auth-wrapper">
        <div class="auth-card">
            <h2 style="font-weight: 800; color: #fff; margin-bottom: 0.5rem; font-size: 1.8rem;">CRIE SUA CONTA</h2>
            <p style="color: #888; margin-bottom: 2rem; font-size: 0.9rem;">Junte-se ao Momentum Fit</p>
          
            <% if (typeof error !== 'undefined' && error && error.length > 0) { %>
                <div style="background: rgba(239, 68, 68, 0.2); color: #fca5a5; padding: 10px; border-radius: 6px; margin-bottom: 1rem; font-size: 0.85rem; text-align: left;">
                    <i class="fas fa-exclamation-circle"></i> <%= error %>
                </div>
            <% } %>

            <form action="/auth/register" method="POST">
                <input type="hidden" name="_csrf" value="<%= typeof csrfToken !== 'undefined' ? csrfToken : '' %>">
                
                <div class="form-group">
                    <label>Nome Completo</label>
                    <input type="text" name="name" required placeholder="Seu nome">
                </div>

                <div class="form-group">
                    <label>Email</label>
                    <input type="email" name="email" required placeholder="seu@email.com">
                </div>

                <div class="form-group">
                    <label>Tipo de Conta</label>
                    <select name="role">
                        <option value="client">Aluno</option>
                        <option value="trainer">Personal Trainer</option>
                    </select>
                </div>

                <div class="form-group">
                    <label>Senha</label>
                    <input type="password" name="password" required placeholder="Mínimo 6 caracteres">
                </div>

                <div class="form-group">
                    <label>Confirmar Senha</label>
                    <input type="password" name="confirmPassword" required placeholder="Repita a senha">
                </div>

                <button type="submit" class="btn-auth">CADASTRAR</button>
            </form>

            <p style="margin-top: 2rem; font-size: 0.9rem; color: #888;">
                Já tem uma conta? <a href="/auth/login" style="color: var(--primary-color); font-weight: 700;">Fazer Login</a>
            </p>
        </div>
    </div>

    <%- include('../partials/footer.ejs') %>
