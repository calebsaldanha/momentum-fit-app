<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cadastro - Momentum Fit</title>
    <link rel="stylesheet" href="/css/styles.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet">
</head>
<body class="auth-page">
    <div class="auth-container">
        <div class="auth-left">
            <div class="logo">
                <img src="/images/momentum-fit-logo-completo.png" alt="Momentum Fit">
            </div>
            <h2>Crie sua conta</h2>
            <p>Comece sua jornada de transformação hoje.</p>
            
            <% if (messages.error) { %>
                <div class="alert alert-danger"><%= messages.error %></div>
            <% } %>

            <form action="/auth/register" method="POST" id="registerForm">
                <input type="hidden" name="plan" value="<%= plan %>">

                <div class="form-group">
                    <label>Nome Completo</label>
                    <input type="text" name="name" required placeholder="Seu nome">
                </div>
                
                <div class="form-group">
                    <label>E-mail</label>
                    <input type="email" name="email" required placeholder="seu@email.com">
                </div>
                
                <div class="form-group">
                    <label>Senha</label>
                    <input type="password" name="password" id="password" required placeholder="Mínimo 6 caracteres">
                    <small id="passwordHelp" style="color: red; display: none;">A senha deve ter pelo menos 6 caracteres.</small>
                </div>

                <div class="form-group">
                    <label>Confirmar Senha</label>
                    <input type="password" name="confirm_password" id="confirm_password" required placeholder="Repita a senha">
                    <small id="confirmHelp" style="color: red; display: none;">As senhas não coincidem.</small>
                </div>
                
                <div class="form-group">
                    <label>Tipo de Conta</label>
                    <select name="role">
                        <option value="client">Aluno (Quero treinar)</option>
                        <option value="trainer">Personal Trainer (Quero dar aulas)</option>
                    </select>
                </div>

                <button type="submit" class="btn-primary btn-block">Cadastrar</button>
            </form>
            
            <p class="auth-footer">
                Já tem conta? <a href="/auth/login">Faça Login</a>
            </p>
        </div>
        <div class="auth-right">
            </div>
    </div>

    <script>
        const form = document.getElementById('registerForm');
        const pass = document.getElementById('password');
        const confirm = document.getElementById('confirm_password');
        const passHelp = document.getElementById('passwordHelp');
        const confirmHelp = document.getElementById('confirmHelp');

        form.addEventListener('submit', function(e) {
            let valid = true;
            
            if (pass.value.length < 6) {
                passHelp.style.display = 'block';
                valid = false;
            } else {
                passHelp.style.display = 'none';
            }

            if (pass.value !== confirm.value) {
                confirmHelp.style.display = 'block';
                valid = false;
            } else {
                confirmHelp.style.display = 'none';
            }

            if (!valid) e.preventDefault();
        });
    </script>
</body>
</html>
