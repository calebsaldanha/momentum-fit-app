<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <%- include('../layouts/public/head', { title: 'Home' }) %>
    <%- include('../layouts/public/header') %>
</head>
<body>
    <section class="hero-section bg-home">
        <div class="hero-overlay"></div>
        <div class="container hero-content">
            <div class="hero-text-wrapper">
                <h1 class="hero-title">
                    <%= getText('home', 'hero', 'title_line1', 'Seu Corpo.') %><br>
                    <span class="text-gradient"><%= getText('home', 'hero', 'title_line2', 'Sua Obra.') %></span>
                </h1>
                <p class="hero-subtitle">
                    <%= getText('home', 'hero', 'subtitle', 'Texto padrão se o banco falhar.') %>
                </p>
                <div class="hero-buttons">
                    <a href="/auth/register" class="btn btn-primary btn-lg glow-effect"><%= getText('home', 'hero', 'cta_primary', 'Começar') %></a>
                    <a href="/plans" class="btn btn-outline btn-lg">Ver Planos</a>
                </div>
            </div>
        </div>
    </section>

    <section class="section-padding bg-dark">
        <div class="container">
            <div class="grid-3">
                <div class="feature-card">
                    <i class="fas fa-brain feature-icon"></i>
                    <h3><%= getText('home', 'features', 'card1_title', 'IA') %></h3>
                    <p><%= getText('home', 'features', 'card1_text', '...') %></p>
                </div>
                <div class="feature-card">
                    <i class="fas fa-chart-line feature-icon"></i>
                    <h3>Dados Reais</h3>
                    <p>Métricas precisas sobre sua evolução de carga e volume.</p>
                </div>
                <div class="feature-card">
                    <i class="fas fa-user-astronaut feature-icon"></i>
                    <h3>Coach 24/7</h3>
                    <p>Dúvidas sobre execução ou dieta? Sua IA responde na hora.</p>
                </div>
            </div>
        </div>
    </section>

    <%- include('../layouts/public/footer') %>
</body>
</html>
