<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <%- include('../layouts/app/head') %>
    <title>Meu Perfil | Momentum Fit</title>
</head>
<body>
    <div class="dashboard-wrapper">
        <%- include('../partials/sidebar') %>

        <div class="main-content">
            <%- include('../partials/app-header') %>

            <div class="content-pad">
                <div class="card" style="max-width: 800px; margin: 0 auto;">
                    <div class="card-header">
                        <h3 class="card-title">Editar Perfil</h3>
                    </div>

                    <%- include('../partials/flash') %>

                    <form action="/client/profile" method="POST">
                        
                        <h4 style="color: var(--text-secondary); text-transform: uppercase; font-size: 0.75rem; letter-spacing: 1px; margin-bottom: 1rem;">Pessoal</h4>
                        <div class="grid-2">
                            <div class="form-group">
                                <label>Nome Completo</label>
                                <input type="text" value="<%= user.name %>" disabled style="opacity: 0.6; cursor: not-allowed;">
                            </div>
                            <div class="form-group">
                                <label>Email</label>
                                <input type="email" value="<%= user.email %>" disabled style="opacity: 0.6; cursor: not-allowed;">
                            </div>
                            <div class="form-group">
                                <label>Telefone</label>
                                <input type="text" name="phone" value="<%= user.phone || '' %>">
                            </div>
                            <div class="form-group">
                                <label>Nascimento</label>
                                <input type="date" name="birth_date" value="<%= profile.birth_date ? new Date(profile.birth_date).toISOString().split('T')[0] : '' %>">
                            </div>
                        </div>

                        <h4 style="color: var(--text-secondary); text-transform: uppercase; font-size: 0.75rem; letter-spacing: 1px; margin: 1.5rem 0 1rem;">Físico & Objetivos</h4>
                        <div class="grid-2">
                            <div class="form-group">
                                <label>Altura (cm)</label>
                                <input type="number" name="height" value="<%= profile.height || '' %>">
                            </div>
                            <div class="form-group">
                                <label>Peso (kg)</label>
                                <input type="number" step="0.1" name="weight" value="<%= profile.weight || '' %>">
                            </div>
                        </div>

                        <div class="form-group">
                            <label>Objetivo Principal</label>
                            <select name="objective">
                                <option value="emagrecimento" <%= profile.objective === 'emagrecimento' ? 'selected' : '' %>>Emagrecimento</option>
                                <option value="hipertrofia" <%= profile.objective === 'hipertrofia' ? 'selected' : '' %>>Hipertrofia</option>
                                <option value="saude" <%= profile.objective === 'saude' ? 'selected' : '' %>>Saúde Geral</option>
                            </select>
                        </div>

                        <div class="form-group">
                            <label>Restrições / Lesões</label>
                            <textarea name="restrictions" rows="3"><%= profile.restrictions || '' %></textarea>
                        </div>

                        <div style="text-align: right; margin-top: 2rem;">
                            <button type="submit" class="btn btn-primary">Salvar Alterações</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
