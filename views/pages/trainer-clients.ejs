<%- include('../layouts/app/head') %>

<div class="dashboard-layout">
    <%- include('../partials/sidebar') %>
    
    <main class="app-main">
        <%- include('../partials/app-header') %>
        
        <div class="dashboard-content">
            <div class="container-fluid">
                
                <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 30px; flex-wrap: wrap; gap: 20px;">
                    <div>
                        <h2 style="color: white; margin-bottom: 5px;">Meus Alunos</h2>
                        <p style="color: #888;">Gerencie o acesso e prescreva treinos.</p>
                    </div>
                    <button onclick="document.getElementById('addClientModal').style.display='flex'" class="btn btn-primary">
                        <i class="fas fa-plus"></i> Adicionar Aluno
                    </button>
                </div>

                <% if (error_msg.length > 0) { %> <div class="alert alert-danger"><%= error_msg %></div> <% } %>
                <% if (success_msg.length > 0) { %> <div class="alert alert-success"><%= success_msg %></div> <% } %>

                <div class="clients-grid">
                    <% if (clients && clients.length > 0) { %>
                        <% clients.forEach(client => { %>
                            <div class="app-card client-card">
                                <div class="client-header">
                                    <div class="avatar">
                                        <% if(client.photo_url) { %>
                                            <img src="<%= client.photo_url %>">
                                        <% } else { %>
                                            <i class="fas fa-user"></i>
                                        <% } %>
                                    </div>
                                    <div class="info">
                                        <h4><%= client.name %></h4>
                                        <p class="email"><%= client.email %></p>
                                    </div>
                                    <div class="status-badge">
                                        <% if (client.current_plan_id) { %>
                                            <span style="color: var(--primary);"><i class="fas fa-check-circle"></i> Ativo</span>
                                        <% } else { %>
                                            <span style="color: #ff6b6b;"><i class="fas fa-exclamation-circle"></i> Inativo</span>
                                        <% } %>
                                    </div>
                                </div>
                                
                                <div class="client-body">
                                    <div class="meta-item">
                                        <i class="fas fa-bullseye"></i>
                                        <span><%= client.objective || 'Sem objetivo' %></span>
                                    </div>
                                    <div class="meta-item">
                                        <i class="fas fa-phone"></i>
                                        <span><%= client.phone || 'Sem telefone' %></span>
                                    </div>
                                </div>

                                <div class="client-actions">
                                    <a href="/trainer/client/<%= client.id %>" class="btn btn-sm btn-outline w-100">Ver Perfil</a>
                                    <form action="/trainer/clients/remove" method="POST" onsubmit="return confirm('Tem certeza?');" style="margin-left: 10px;">
                                        <input type="hidden" name="clientId" value="<%= client.id %>">
                                        <button type="submit" class="btn btn-sm btn-outline" style="border-color: #ff6b6b; color: #ff6b6b;">
                                            <i class="fas fa-trash"></i>
                                        </button>
                                    </form>
                                </div>
                            </div>
                        <% }) %>
                    <% } else { %>
                        <div style="grid-column: 1 / -1; text-align: center; padding: 60px; background: #111; border-radius: 12px; border: 1px dashed #333;">
                            <i class="fas fa-users" style="font-size: 3rem; color: #333; margin-bottom: 20px;"></i>
                            <h4 style="color: #666;">Nenhum aluno encontrado</h4>
                            <p style="color: #444;">Utilize o botão acima para adicionar alunos pelo e-mail cadastrado.</p>
                        </div>
                    <% } %>
                </div>

            </div>
        </div>
    </main>
</div>

<div id="addClientModal" class="modal-overlay" style="display: none;">
    <div class="modal-content">
        <div style="display: flex; justify-content: space-between; margin-bottom: 20px;">
            <h3 style="color: white;">Adicionar Aluno</h3>
            <button onclick="document.getElementById('addClientModal').style.display='none'" style="background: none; border: none; color: #666; font-size: 1.5rem; cursor: pointer;">&times;</button>
        </div>
        <p style="color: #888; margin-bottom: 20px; font-size: 0.9rem;">
            Digite o e-mail do aluno. O aluno já deve ter criado uma conta na plataforma como "Aluno".
        </p>
        <form action="/trainer/clients/add" method="POST">
            <div style="margin-bottom: 20px;">
                <label style="color: #ccc; margin-bottom: 5px; display: block;">E-mail do Aluno</label>
                <input type="email" name="email" required placeholder="exemplo@email.com" 
                       style="width: 100%; padding: 12px; background: #0a0a0a; border: 1px solid #333; border-radius: 6px; color: white;">
            </div>
            <button type="submit" class="btn btn-primary w-100">Vincular</button>
        </form>
    </div>
</div>

<style>
    /* Grid Responsivo de Cards */
    .clients-grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
        gap: 20px;
    }
    
    .client-card {
        background: #111; border: 1px solid #222; border-radius: 12px; padding: 20px;
        display: flex; flex-direction: column; transition: 0.2s;
    }
    .client-card:hover { border-color: #333; transform: translateY(-2px); }

    .client-header { display: flex; align-items: center; gap: 15px; margin-bottom: 20px; position: relative; }
    .avatar { width: 50px; height: 50px; background: #222; border-radius: 50%; display: flex; align-items: center; justify-content: center; overflow: hidden; color: #666; }
    .avatar img { width: 100%; height: 100%; object-fit: cover; }
    .info h4 { color: white; font-size: 1rem; margin: 0; }
    .info .email { color: #666; font-size: 0.85rem; margin: 0; }
    .status-badge { position: absolute; top: 0; right: 0; font-size: 0.75rem; }

    .client-body { margin-bottom: 20px; flex: 1; }
    .meta-item { display: flex; align-items: center; gap: 10px; color: #888; font-size: 0.9rem; margin-bottom: 8px; }
    .meta-item i { width: 20px; text-align: center; color: #444; }

    .client-actions { display: flex; gap: 10px; border-top: 1px solid #222; padding-top: 15px; }

    /* Modal */
    .modal-overlay {
        position: fixed; top: 0; left: 0; width: 100%; height: 100%;
        background: rgba(0,0,0,0.8); backdrop-filter: blur(5px);
        z-index: 9999; justify-content: center; align-items: center;
    }
    .modal-content {
        background: #151515; border: 1px solid #333; padding: 30px; border-radius: 16px;
        width: 100%; max-width: 400px; box-shadow: 0 20px 50px rgba(0,0,0,0.5);
    }
</style>
