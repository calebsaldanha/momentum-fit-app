<%- include('../partials/header.ejs', { title: 'Avaliação Inicial', bodyClass: 'dashboard-body' }) %>
<div class="dashboard-container">
    <div style="width: 100%; max-width: 900px; margin: 0 auto; padding: 2rem;">
        
        <div class="section-header">
            <h1>Avaliação Inicial</h1>
            <p>Preencha os dados abaixo para que possamos montar seu plano.</p>
        </div>

        <% if (typeof error !== 'undefined' && error) { %>
            <div class="alert alert-error"><%= error %></div>
        <% } %>

        <form action="/client/initial-form" method="POST" class="dashboard-form">
            <input type="hidden" name="_csrf" value="<%= csrfToken %>">

            <div class="section-card">
                <h3 style="color: var(--c-accent-primary); border-bottom: 1px solid #333; padding-bottom: 10px; margin-bottom: 15px;">1. Dados Pessoais</h3>
                
                <div style="display: grid; grid-template-columns: 1fr 1fr 1fr; gap: 1rem;">
                    <div class="form-group">
                        <label>Idade *</label>
                        <input type="number" name="age" required value="<%= profile && profile.age %>">
                        <small style="color: #666; font-size: 0.8rem;">Sua idade atual em anos.</small>
                    </div>
                    <div class="form-group">
                        <label>Telefone *</label>
                        <input type="text" name="phone" required placeholder="(99) 99999-9999" value="<%= profile && profile.phone %>">
                        <small style="color: #666; font-size: 0.8rem;">Para contato pelo WhatsApp.</small>
                    </div>
                    <div class="form-group">
                        <label>Gênero</label>
                        <select name="gender_identity">
                            <option value="">Selecione...</option>
                            <option value="M">Masculino</option>
                            <option value="F">Feminino</option>
                            <option value="NB">Não-binário</option>
                            <option value="O">Outro</option>
                        </select>
                        <small style="color: #666; font-size: 0.8rem;">Como você se identifica.</small>
                    </div>
                </div>
            </div>

            <div class="section-card">
                <h3 style="color: var(--c-accent-primary); border-bottom: 1px solid #333; padding-bottom: 10px; margin-bottom: 15px;">2. Medidas & Corpo</h3>
                <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 1rem;">
                    <div class="form-group">
                        <label>Peso (kg) *</label>
                        <input type="text" name="weight" required placeholder="Ex: 70.5" value="<%= profile && profile.weight %>">
                        <small style="color: #666; font-size: 0.8rem;">Use ponto ou vírgula. Ex: 70,5</small>
                    </div>
                    <div class="form-group">
                        <label>Altura (m) *</label>
                        <input type="text" name="height" required placeholder="Ex: 1.75" value="<%= profile && profile.height %>">
                        <small style="color: #666; font-size: 0.8rem;">Em metros (1.75) ou cm (175).</small>
                    </div>
                </div>
            </div>

            <div class="section-card">
                <h3 style="color: var(--c-accent-primary); border-bottom: 1px solid #333; padding-bottom: 10px; margin-bottom: 15px;">3. Objetivos & Rotina</h3>
                <div class="form-group">
                    <label>Objetivo Principal *</label>
                    <input type="text" name="main_goal" required placeholder="Ex: Perder peso, Ganhar massa..." value="<%= profile && profile.main_goal %>">
                    <small style="color: #666; font-size: 0.8rem;">O que você mais deseja alcançar?</small>
                </div>
                
                <div class="form-group">
                    <label>Nível de Atividade *</label>
                    <select name="fitness_level" required>
                        <option value="sedentary">Sedentário (Não treino atualmente)</option>
                        <option value="beginner">Iniciante (Treino 1-2x na semana)</option>
                        <option value="intermediate">Intermediário (Treino regular há 6 meses)</option>
                        <option value="advanced">Avançado (Treino pesado há anos)</option>
                    </select>
                    <small style="color: #666; font-size: 0.8rem;">Seja sincero para ajustarmos a intensidade.</small>
                </div>

                <div class="form-group">
                    <label>Histórico de Saúde</label>
                    <textarea name="medical_conditions" rows="2" placeholder="Tem alguma doença, lesão antiga ou toma remédio controlado?"><%= profile && profile.medical_conditions %></textarea>
                    <small style="color: #666; font-size: 0.8rem;">Detalhe qualquer condição que o treinador precise saber.</small>
                </div>
            </div>

            <button type="submit" class="btn btn-primary" style="width: 100%; margin-top: 20px;">ENVIAR AVALIAÇÃO</button>
        </form>
    </div>
</div>
<%- include('../partials/footer.ejs') %>
