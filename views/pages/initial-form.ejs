<%- include('../partials/header.ejs', { title: 'Complete seu Perfil - Momentum Fit', bodyClass: 'dashboard-body' }) %>

<div class="dashboard-container" style="grid-template-columns: 1fr; max-width: 800px;">
    <main class="dashboard-main">
      <div class="section">
            <div class="section-header" style="text-align: center; flex-direction: column;">
                <h1>Seu Perfil, Seu Momentum</h1>
                <p>Estas informações nos ajudam a criar o plano de treino ideal para você.</p>
            </div>

            <% if (typeof error !== 'undefined' && error) { %>
                <div class="alert alert-error"><%= error %></div>
            <% } %>

            <form method="POST" action="/client/initial-form">
                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                <div class="form-row">
                    <div class="form-group">
                        <label for="age">Idade</label>
                        <input type="number" id="age" name="age" min="16" max="100" required>
                    </div>
                    <div class="form-group">
                        <label for="fitness_level">Seu nível de experiência com exercícios</label>
                        <select id="fitness_level" name="fitness_level" required>
                            <option value="">Selecione uma opção</option>
                            <option value="beginner">Iniciante (começando agora ou com pouca experiência)</option>
                            <option value="intermediate">Intermediário (já treino há um tempo)</option>
                            <option value="advanced">Avançado (treino com frequência e consistência)</option>
                        </select>
                    </div>
                </div>
                
                <!-- Campos de gênero e pronomes removidos para a plataforma geral -->
                
                <div class="form-row">
                    <div class="form-group">
                        <label for="weight">Peso (kg) (opcional)</label>
                        <input type="number" id="weight" name="weight" step="0.1" min="30" max="200">
                    </div>
                    <div class="form-group">
                        <label for="height">Altura (cm) (opcional)</label>
                        <input type="number" id="height" name="height" min="100" max="250">
                    </div>
                </div>

                <div class="form-group">
                    <label for="goals">Quais são seus objetivos principais com o treino?</label>
                    <textarea id="goals" name="goals" rows="4" placeholder="Ex: Ganho de massa muscular, emagrecimento, mais energia no dia a dia, preparação para uma prova, etc." required></textarea>
                </div>

                <!-- Campo de transição removido para a plataforma geral -->
              
                <div class="form-group">
                    <label for="medical_conditions">Você tem alguma condição médica, lesão ou limitação?</label>
                    <textarea id="medical_conditions" name="medical_conditions" rows="3" placeholder="Nenhuma, se não tiver."></textarea>
                </div>

                <button type="submit" class="btn btn-primary" style="width: 100%;">Salvar e Iniciar Jornada</button>
            </form>
        </div>
    </main>
</div>
<%- include('../partials/footer.ejs') %>
