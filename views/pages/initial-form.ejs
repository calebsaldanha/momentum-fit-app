<%- include('../partials/header') %>

<div class="auth-page-wrapper" style="min-height: calc(100vh - 70px); display: flex; align-items: center; justify-content: center; padding: 2rem 1rem;">
    <div class="auth-card" style="max-width: 800px; width: 100%;">
        <div class="auth-header">
            <h2>Anamnese Inicial</h2>
            <p>Precisamos conhecer você melhor para montar o treino perfeito.</p>
        </div>

        <form action="/client/initial-form" method="POST">
            <input type="hidden" name="_csrf" value="<%= typeof csrfToken !== 'undefined' ? csrfToken : '' %>">

            <h3 class="form-section-title"><i class="fas fa-bullseye"></i> Objetivos Principais</h3>
            
            <div class="form-grid">
                <div class="form-group form-full-width">
                    <label class="form-label">Qual é o seu objetivo principal hoje?</label>
                    <select name="primaryGoal" class="form-input" required>
                        <option value="" disabled selected>Selecione uma opção...</option>
                        <option value="weight_loss">Perda de Peso / Emagrecimento</option>
                        <option value="hypertrophy">Hipertrofia (Ganho de Massa)</option>
                        <option value="strength">Força e Condicionamento</option>
                        <option value="health">Saúde e Qualidade de Vida</option>
                        <option value="performance">Performance Esportiva</option>
                    </select>
                </div>

                <div class="form-group">
                    <label class="form-label">Frequência Semanal Desejada</label>
                    <select name="frequency" class="form-input" required>
                        <option value="2">2 vezes por semana</option>
                        <option value="3">3 vezes por semana</option>
                        <option value="4">4 vezes por semana</option>
                        <option value="5">5 vezes por semana</option>
                        <option value="6">6 vezes por semana</option>
                    </select>
                </div>

                <div class="form-group">
                    <label class="form-label">Tempo Disponível por Treino</label>
                    <select name="duration" class="form-input" required>
                        <option value="30">30 minutos</option>
                        <option value="45">45 minutos</option>
                        <option value="60">60 minutos (1 hora)</option>
                        <option value="90">Mais de 1 hora</option>
                    </select>
                </div>
            </div>

            <h3 class="form-section-title"><i class="fas fa-heartbeat"></i> Histórico de Saúde</h3>
            
            <div class="form-grid">
                <div class="form-group form-full-width">
                    <label class="form-label">Possui alguma lesão ou limitação física?</label>
                    <textarea name="injuries" class="form-input" rows="2" placeholder="Ex: Dor no joelho direito, cirurgia no ombro em 2020... (Deixe em branco se não houver)"></textarea>
                </div>

                <div class="form-group form-full-width">
                    <label class="form-label">Toma algum medicamento de uso contínuo?</label>
                    <textarea name="medications" class="form-input" rows="2" placeholder="Liste os medicamentos, se houver..."></textarea>
                </div>

                <div class="form-group">
                    <label class="form-label">Como você avalia seu nível de estresse?</label>
                    <select name="stressLevel" class="form-input">
                        <option value="low">Baixo</option>
                        <option value="medium">Médio</option>
                        <option value="high">Alto</option>
                    </select>
                </div>

                <div class="form-group">
                    <label class="form-label">Média de horas de sono por noite</label>
                    <input type="number" name="sleepHours" class="form-input" placeholder="Ex: 7" min="1" max="24">
                </div>
            </div>

            <h3 class="form-section-title"><i class="fas fa-walking"></i> Estilo de Vida</h3>

            <div class="form-grid">
                <div class="form-group">
                    <label class="form-label">Ocupação / Trabalho</label>
                    <select name="occupationType" class="form-input">
                        <option value="sedentary">Sedentário (Escritório/Sentado)</option>
                        <option value="lightly_active">Levemente Ativo (Em pé/Andando pouco)</option>
                        <option value="active">Ativo (Movimento constante)</option>
                        <option value="very_active">Muito Ativo (Trabalho físico pesado)</option>
                    </select>
                </div>

                <div class="form-group">
                    <label class="form-label">Experiência com Treinos</label>
                    <select name="experienceLevel" class="form-input" required>
                        <option value="beginner">Iniciante (Nunca treinei / Parado há muito tempo)</option>
                        <option value="intermediate">Intermediário (Treino há 6 meses - 2 anos)</option>
                        <option value="advanced">Avançado (Treino há mais de 2 anos)</option>
                    </select>
                </div>
            </div>

            <button type="submit" class="btn btn-primary btn-full" style="margin-top: 2rem;">
                Salvar Perfil e Continuar <i class="fas fa-arrow-right"></i>
            </button>
        </form>
    </div>
</div>

<%- include('../partials/footer') %>
