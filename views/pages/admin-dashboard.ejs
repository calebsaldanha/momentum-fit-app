<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <%- include('../layouts/app/head') %>
    <title>Admin | Momentum Fit</title>
</head>
<body>
    <div class="dashboard-wrapper">
        <%- include('../partials/sidebar') %>
        <div class="main-content">
            <%- include('../partials/app-header') %>
            
            <div class="content-pad">
                <h1 style="font-size: 1.5rem; margin-bottom: 2rem;">Visão Geral Administrativa</h1>

                <div class="grid-3">
                    <div class="card">
                        <div style="color: var(--text-secondary);">Receita Total</div>
                        <div style="font-size: 2rem; font-weight: 700; color: var(--success);">R$ <%= revenue.total || '0.00' %></div>
                    </div>
                    <div class="card">
                        <div style="color: var(--text-secondary);">Usuários Ativos</div>
                        <div style="font-size: 2rem; font-weight: 700; color: var(--neon-blue);"><%= users_count %></div>
                    </div>
                </div>

                <div class="card">
                    <div class="card-header">
                        <h3 class="card-title">Manutenção do Sistema</h3>
                    </div>
                    <p style="color: var(--text-secondary); margin-bottom: 1rem;">Ferramentas de reparo para o banco de dados.</p>
                    <form action="/admin/repair-db" method="POST">
                        <button type="submit" class="btn btn-outline" style="color: var(--warning); border-color: var(--warning);">
                            <i class="fas fa-tools"></i> Rodar Auto-Repair
                        </button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
