<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - Momentum Fit</title>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/styles.css">
    <style>
        .auth-body { 
            background: url('/images/auth-bg.png') no-repeat center center/cover;
            min-height: 100vh; 
            display: flex; 
            flex-direction: column;
            position: relative;
        }
        .auth-overlay { position: absolute; inset: 0; background: rgba(0,0,0,0.7); z-index: 1; }
        
        .auth-wrapper {
            flex: 1;
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
            z-index: 2;
            padding: 4rem 1rem;
        }

        .auth-card { 
            background: rgba(20, 20, 20, 0.95);
            padding: 3rem; 
            border-radius: 12px; 
            border: 1px solid rgba(255,255,255,0.1); 
            width: 100%; 
            max-width: 450px; 
            text-align: center; 
            box-shadow: 0 20px 50px rgba(0,0,0,0.5);
        }
        .form-group { margin-bottom: 1.5rem; text-align: left; }
        .form-group label { display: block; font-size: 0.85rem; color: #ccc; margin-bottom: 5px; }
        .form-group input { 
            width: 100%;
            padding: 12px; background: #0a0a0a; border: 1px solid #333; 
            color: #fff; border-radius: 6px; outline: none; transition: 0.3s;
        }
        .form-group input:focus { border-color: var(--c-accent-primary); }
        
        footer { position: relative; z-index: 2; background: rgba(0,0,0,0.8); border-top: 1px solid rgba(255,255,255,0.1); }
    </style>
</head>
<body class="auth-body">
    <div class="auth-overlay"></div>
    
    <div class="auth-wrapper">
        <div class="auth-card">
            <a href="/" style="text-decoration: none;">
                <h2 style="font-weight: 800; color: #fff; margin-bottom: 2rem; font-size: 1.8rem;">MOMENTUM <span style="color: var(--c-accent-primary);">FIT</span></h2>
            </a>
            
            <% if (typeof error !== 'undefined' && error && error.length > 0) { %>
                <div style="background: rgba(239, 68, 68, 0.2); color: #fca5a5; padding: 10px; border-radius: 6px; margin-bottom: 1rem; font-size: 0.9rem;">
                    <%= error %>
                </div>
            <% } %>

            <form action="/auth/login" method="POST">
                <input type="hidden" name="_csrf" value="<%= typeof csrfToken !== 'undefined' ? csrfToken : '' %>">
                
                <div class="form-group">
                    <label>Email</label>
                    <input type="email" name="email" required placeholder="seu@email.com">
                </div>

                <div class="form-group">
                    <label>Senha</label>
                    <input type="password" name="password" required placeholder="Sua senha">
                </div>

                <button type="submit" class="btn btn-primary" style="width: 100%; padding: 12px; font-weight: 700;">ENTRAR</button>
            </form>

            <div style="margin-top: 1.5rem; display: flex; justify-content: space-between; font-size: 0.85rem;">
                <a href="/auth/forgot-password" style="color: #888;">Esqueci minha senha</a>
            </div>

            <p style="margin-top: 2rem; font-size: 0.9rem; color: #888;">
                Ainda não tem conta? <a href="/auth/register" style="color: var(--c-accent-primary);">Cadastre-se</a>
            </p>
        </div>
    </div>

    <%- include('../partials/footer.ejs') %>
