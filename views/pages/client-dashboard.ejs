<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <%- include('../layouts/app/head') %>
    <title>Painel | Momentum Fit</title>
</head>
<body>
    <div class="dashboard-wrapper">
        <%- include('../partials/sidebar') %>

        <div class="main-content">
            <%- include('../partials/app-header') %>

            <div class="content-pad">
                <div class="welcome-banner">
                    <h1 style="font-size: 1.8rem; margin-bottom: 0.5rem;">Ol√°, <%= user.name.split(' ')[0] %>! Ì∫Ä</h1>
                    <p style="opacity: 0.9;">Vamos manter o foco hoje? Seu plano atual √©: <strong><%= stats.plan_name %></strong></p>
                </div>

                <div class="grid-3">
                    <div style="grid-column: span 2;">
                        <div class="card">
                            <div class="card-header">
                                <h3 class="card-title"><i class="fas fa-dumbbell" style="color:var(--neon-blue); margin-right:8px;"></i> Treino de Hoje</h3>
                            </div>
                            
                            <% if (nextWorkout) { %>
                                <div style="background: rgba(255,255,255,0.03); padding: 1.5rem; border-radius: var(--radius); border: 1px solid rgba(255,255,255,0.05);">
                                    <h2 style="font-size: 1.4rem; color: white; margin-bottom: 0.5rem;"><%= nextWorkout.name %></h2>
                                    <p style="color: var(--text-secondary); margin-bottom: 1.5rem;"><%= nextWorkout.description || 'Foco e consist√™ncia.' %></p>
                                    
                                    <div style="display:flex; gap: 1.5rem; margin-bottom: 1.5rem; font-size: 0.9rem; color: var(--text-secondary);">
                                        <span><i class="fas fa-list"></i> <%= nextWorkout.exercises_count %> Exerc√≠cios</span>
                                        <span><i class="fas fa-clock"></i> ~45 min</span>
                                    </div>

                                    <a href="/client/workouts/<%= nextWorkout.id %>" class="btn btn-primary btn-block">
                                        Iniciar Treino Agora
                                    </a>
                                </div>
                            <% } else { %>
                                <div style="text-align: center; padding: 2rem;">
                                    <i class="fas fa-check-circle" style="font-size: 3rem; color: var(--text-secondary); opacity: 0.5; margin-bottom: 1rem;"></i>
                                    <p style="color: var(--text-secondary);">Nenhum treino pendente para hoje.</p>
                                </div>
                            <% } %>
                        </div>
                    </div>

                    <div>
                        <div class="card">
                            <h3 class="card-title" style="margin-bottom: 1rem;">Seu Progresso</h3>
                            <div style="display: flex; justify-content: space-between; align-items: flex-end;">
                                <div>
                                    <div style="font-size: 2.5rem; font-weight: 700; color: var(--neon-purple); line-height: 1;"><%= stats.total_workouts %></div>
                                    <div style="font-size: 0.85rem; color: var(--text-secondary);">Treinos totais</div>
                                </div>
                                <i class="fas fa-chart-bar" style="font-size: 2rem; color: rgba(255,255,255,0.1);"></i>
                            </div>
                        </div>

                        <% if(trainer) { %>
                        <div class="card">
                            <h3 class="card-title" style="margin-bottom: 1rem;">Seu Treinador</h3>
                            <div style="display: flex; align-items: center; gap: 1rem;">
                                <% if(trainer.photo_url) { %>
                                    <img src="<%= trainer.photo_url %>" style="width: 50px; height: 50px; border-radius: 50%;">
                                <% } else { %>
                                    <div style="width: 50px; height: 50px; border-radius: 50%; background: var(--bg-surface-hover); display: flex; align-items: center; justify-content: center;">
                                        <%= trainer.name.charAt(0) %>
                                    </div>
                                <% } %>
                                <div>
                                    <div style="font-weight: 600;"><%= trainer.name %></div>
                                    <a href="/client/chat" style="font-size: 0.85rem; color: var(--neon-blue);">Enviar mensagem</a>
                                </div>
                            </div>
                        </div>
                        <% } %>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
