<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <%- include('../layouts/app/head') %>
    <title>Painel | Momentum Fit</title>
</head>
<body>
    <div class="dashboard-wrapper">
        <%- include('../partials/sidebar') %>

        <div class="main-content">
            <%- include('../partials/app-header', { title: 'Visão Geral' }) %>

            <div class="content-pad">
                <div class="welcome-banner">
                    <div style="position: relative; z-index: 2;">
                        <h1>Vamos esmagar, <%= user.name.split(' ')[0] %>! ⚡</h1>
                        <p style="opacity: 0.9;">
                            Seu foco hoje está em <span style="color: var(--neon-primary); font-weight: 700;">Hipertrofia</span>. 
                            Plano atual: <span class="badge badge-neutral"><%= stats.plan_name || 'Free' %></span>
                        </p>
                    </div>
                </div>

                <div class="grid-3">
                    <div style="grid-column: span 2;">
                        
                        <div class="card">
                            <div class="card-header">
                                <h3 class="card-title"><i class="fas fa-fire"></i> Treino do Dia</h3>
                                <span class="badge badge-success">PRIORIDADE</span>
                            </div>

                            <% if (nextWorkout) { %>
                                <div style="display: flex; flex-direction: column; gap: 20px;">
                                    <div>
                                        <h2 style="font-size: 1.8rem; margin-bottom: 5px;"><%= nextWorkout.name %></h2>
                                        <p><%= nextWorkout.description || 'Siga o plano. Respeite o descanso.' %></p>
                                    </div>

                                    <div style="display: flex; gap: 30px; padding: 20px; background: rgba(255,255,255,0.02); border-radius: 8px;">
                                        <div>
                                            <span style="display: block; font-size: 1.5rem; font-weight: 800; color: var(--neon-primary);">
                                                <%= nextWorkout.exercises_count || 0 %>
                                            </span>
                                            <span style="font-size: 0.8rem; color: var(--text-muted); text-transform: uppercase;">Exercícios</span>
                                        </div>
                                        <div>
                                            <span style="display: block; font-size: 1.5rem; font-weight: 800; color: white;">45-60</span>
                                            <span style="font-size: 0.8rem; color: var(--text-muted); text-transform: uppercase;">Minutos Est.</span>
                                        </div>
                                    </div>

                                    <a href="/client/workouts/<%= nextWorkout.id %>" class="btn btn-primary btn-block">
                                        <i class="fas fa-play" style="margin-right: 10px;"></i> INICIAR SESSÃO
                                    </a>
                                </div>
                            <% } else { %>
                                <div style="text-align: center; padding: 40px 0;">
                                    <i class="fas fa-check-circle" style="font-size: 3rem; color: #222; margin-bottom: 20px;"></i>
                                    <h3 style="color: #666;">Descanso Merecido</h3>
                                    <p>Nenhum treino agendado para hoje.</p>
                                    <a href="/client/workouts" class="btn btn-outline" style="margin-top: 20px;">Ver Biblioteca</a>
                                </div>
                            <% } %>
                        </div>

                        <div class="card">
                            <div class="card-header">
                                <h3 class="card-title"><i class="fas fa-chart-line"></i> Evolução de Carga</h3>
                            </div>
                            <div style="height: 200px; display: flex; align-items: flex-end; justify-content: space-between; gap: 10px; padding-bottom: 10px;">
                                <% [40, 55, 45, 70, 65, 85, 90].forEach(h => { %>
                                    <div style="width: 100%; background: linear-gradient(0deg, var(--neon-subtle) 0%, var(--neon-primary) 100%); height: <%= h %>%; border-radius: 4px; opacity: 0.8;"></div>
                                <% }) %>
                            </div>
                            <p style="text-align: center; font-size: 0.8rem; margin-top: 10px;">Últimos 7 dias</p>
                        </div>
                    </div>

                    <div style="display: flex; flex-direction: column; gap: 30px;">
                        
                        <div class="card" style="background: radial-gradient(circle at top right, rgba(190, 242, 2, 0.1), transparent);">
                            <h3 class="card-title" style="color: var(--neon-primary); margin-bottom: 5px;">TOTAL TREINOS</h3>
                            <div style="font-size: 3.5rem; font-weight: 800; line-height: 1;"><%= stats.total_workouts || 0 %></div>
                            <p style="font-size: 0.85rem; margin-top: 10px;">Sessões concluídas</p>
                        </div>

                        <div class="card">
                            <div class="card-header">
                                <h3 class="card-title"><i class="fas fa-user-astronaut"></i> Seu Treinador</h3>
                            </div>
                            <% if(trainer) { %>
                                <div style="display: flex; align-items: center; gap: 15px; margin-bottom: 20px;">
                                    <div class="user-avatar" style="width: 60px; height: 60px; font-size: 1.5rem;">
                                        <%= trainer.name.charAt(0) %>
                                    </div>
                                    <div>
                                        <div style="font-weight: 700; font-size: 1.1rem;"><%= trainer.name %></div>
                                        <div style="font-size: 0.8rem; color: var(--neon-primary);">Personal Pro</div>
                                    </div>
                                </div>
                                <a href="/client/chat" class="btn btn-outline btn-block">Enviar Mensagem</a>
                            <% } else { %>
                                <div style="text-align: center;">
                                    <p style="margin-bottom: 20px;">Você está usando o <strong>Momentum AI</strong>.</p>
                                    <a href="/client/ai-coach" class="btn btn-primary btn-block" style="color: black;">Falar com IA</a>
                                </div>
                            <% } %>
                        </div>
                        
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
