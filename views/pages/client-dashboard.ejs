<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <%- include('../layouts/app/head') %>
    <title>Painel | Momentum Fit</title>
</head>
<body>
    <div class="dashboard-wrapper">
        <%- include('../partials/sidebar') %>

        <div class="main-content">
            <%- include('../partials/app-header') %>

            <div class="content-pad">
                <div class="welcome-banner">
                    <div style="position: relative; z-index: 2;">
                        <h1>Vamos treinar, <%= user.name.split(' ')[0] %>?</h1>
                        <p style="color: rgba(255,255,255,0.9); font-size: 1.1rem; margin-top: 0.5rem;">
                            Seu plano: <span style="background: rgba(0,0,0,0.3); padding: 2px 8px; border-radius: 4px; font-weight: bold;"><%= stats.plan_name %></span>
                        </p>
                    </div>
                </div>

                <div class="grid-3">
                    <div style="grid-column: span 2;">
                        <div class="card">
                            <div class="card-header">
                                <h3 class="card-title"><i class="fas fa-fire"></i> Treino do Dia</h3>
                                <span class="badge badge-success">HOJE</span>
                            </div>
                            
                            <% if (nextWorkout) { %>
                                <div style="display: flex; flex-direction: column; gap: 1rem;">
                                    <div>
                                        <h2 style="font-size: 1.5rem; color: var(--text-white); margin-bottom: 0.5rem;"><%= nextWorkout.name %></h2>
                                        <p style="color: var(--text-muted);"><%= nextWorkout.description || 'Foco total na execução.' %></p>
                                    </div>
                                    
                                    <div style="display:flex; gap: 2rem; border-top: 1px solid rgba(255,255,255,0.05); padding-top: 1rem; margin-top: 0.5rem;">
                                        <div style="text-align: center;">
                                            <span style="display: block; font-weight: bold; font-size: 1.2rem; color: var(--neon-primary);"><%= nextWorkout.exercises_count %></span>
                                            <span style="font-size: 0.8rem; color: var(--text-muted);">Exercícios</span>
                                        </div>
                                        <div style="text-align: center;">
                                            <span style="display: block; font-weight: bold; font-size: 1.2rem; color: var(--text-white);">45</span>
                                            <span style="font-size: 0.8rem; color: var(--text-muted);">Minutos</span>
                                        </div>
                                    </div>

                                    <a href="/client/workouts/<%= nextWorkout.id %>" class="btn btn-primary btn-block" style="margin-top: 1rem;">
                                        <i class="fas fa-play" style="margin-right: 8px;"></i> INICIAR TREINO
                                    </a>
                                </div>
                            <% } else { %>
                                <div style="text-align: center; padding: 3rem;">
                                    <i class="fas fa-check-circle" style="font-size: 3rem; color: var(--bg-input); margin-bottom: 1rem;"></i>
                                    <p>Tudo feito por hoje! Descanse.</p>
                                </div>
                            <% } %>
                        </div>
                    </div>

                    <div style="display: flex; flex-direction: column; gap: 1.5rem;">
                        <div class="card" style="background: linear-gradient(180deg, rgba(16, 185, 129, 0.05) 0%, transparent 100%); border-color: rgba(16, 185, 129, 0.2);">
                            <h3 class="card-title" style="margin-bottom: 0.5rem; font-size: 0.9rem; text-transform: uppercase; color: var(--neon-primary);">Total Realizado</h3>
                            <div style="font-size: 3rem; font-weight: 800; line-height: 1;"><%= stats.total_workouts %></div>
                            <p style="font-size: 0.8rem; margin-top: 0.5rem;">Treinos concluídos</p>
                        </div>

                        <% if(trainer) { %>
                        <div class="card">
                            <div style="display: flex; align-items: center; gap: 1rem; margin-bottom: 1rem;">
                                <% if(trainer.photo_url) { %>
                                    <img src="<%= trainer.photo_url %>" style="width: 50px; height: 50px; border-radius: 50%; border: 2px solid var(--neon-primary);">
                                <% } else { %>
                                    <div style="width: 50px; height: 50px; border-radius: 50%; background: var(--bg-input); display: flex; align-items: center; justify-content: center; font-weight: bold; color: var(--neon-primary); border: 1px solid rgba(255,255,255,0.1);">
                                        <%= trainer.name.charAt(0) %>
                                    </div>
                                <% } %>
                                <div>
                                    <div style="font-weight: 600; font-size: 0.95rem;"><%= trainer.name %></div>
                                    <div style="font-size: 0.75rem; color: var(--text-muted);">Personal Trainer</div>
                                </div>
                            </div>
                            <a href="/client/ai-coach" class="btn btn-outline btn-block" style="font-size: 0.8rem; padding: 0.5rem;">
                                Mensagem
                            </a>
                        </div>
                        <% } %>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
