<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard - Momentum</title>
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
</head>
<body>

<div class="app-layout">
    <%- include('../partials/sidebar.ejs') %>

    <main class="app-content">
        <%- include('../partials/app-header.ejs', { title: 'Visão Geral' }) %>

        <div class="dashboard-wrapper">
            <div style="margin-bottom: 25px;">
                <h1 style="font-size: 1.5rem; color: white;">Olá, <%= user.name.split(' ')[0] %>!</h1>
                <p style="color: #888; font-size: 0.9rem;">Seu progresso esta semana.</p>
            </div>

            <div class="stats-grid">
                <div class="stat-card">
                    <div class="stat-value"><%= stats.workoutsCompleted || 0 %></div>
                    <div class="stat-label">Treinos Feitos</div>
                </div>
                <div class="stat-card">
                    <div class="stat-value"><%= stats.streak || 0 %></div>
                    <div class="stat-label">Dias Seguidos</div>
                </div>
                <div class="stat-card">
                    <div class="stat-value"><%= stats.currentWeight || '--' %>kg</div>
                    <div class="stat-label">Peso Atual</div>
                </div>
            </div>

            <div class="main-dashboard-grid">
                <div>
                    <div class="content-card" style="border-left: 4px solid var(--primary-color);">
                        <h3 style="color: white; margin-bottom: 15px; font-size: 1.1rem;">Próximo Treino</h3>
                        
                        <% if (nextWorkout) { %>
                            <h2 style="font-size: 1.6rem; color: white; margin-bottom: 5px;"><%= nextWorkout.title %></h2>
                            <p style="color: #aaa; margin-bottom: 20px;"><%= nextWorkout.category %></p>
                            
                            <div style="display: flex; gap: 20px; margin-bottom: 25px; font-size: 0.9rem; color: #888;">
                                <span><i class="far fa-clock"></i> <%= nextWorkout.duration %> min</span>
                                <span><i class="fas fa-dumbbell"></i> <%= nextWorkout.exerciseCount %> exercícios</span>
                            </div>

                            <a href="/workouts/start/<%= nextWorkout.id %>" class="btn btn-primary" style="width: 100%;">
                                Iniciar Treino
                            </a>
                        <% } else { %>
                            <p style="color: #888;">Nenhum treino agendado para hoje.</p>
                            <a href="/client/workouts" class="btn btn-outline btn-sm" style="margin-top: 10px;">Ver Treinos</a>
                        <% } %>
                    </div>
                </div>

                <div>
                    <div class="content-card">
                        <h3 style="color: white; margin-bottom: 15px; font-size: 1.1rem;">Acesso Rápido</h3>
                        <div style="display: flex; flex-direction: column; gap: 10px;">
                            <a href="/client/evolution" class="btn btn-outline" style="justify-content: flex-start; border-color: #333; color: #ccc;">
                                <i class="fas fa-weight"></i> Registrar Peso
                            </a>
                            <a href="/client/ai-coach" class="btn btn-outline" style="justify-content: flex-start; border-color: #333; color: #ccc;">
                                <i class="fas fa-robot"></i> Falar com IA
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </main>
</div>

</body>
</html>
