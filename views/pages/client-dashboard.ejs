<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard | Momentum Fit</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <div class="dashboard-container">
        <%- include('../partials/sidebar') %>
        
        <main class="dashboard-main">
            <div class="page-header" style="text-align: left; padding: 0 0 30px 0; background: transparent; border: none;">
                <h1>Ol√°, <span class="text-neon"><%= user.name.split(' ')[0] %></span>!</h1>
                <p>Vamos manter o foco hoje.</p>
            </div>

            <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(240px, 1fr)); gap: 20px; margin-bottom: 30px;">
                <div class="card-box">
                    <div style="color: #888; font-size: 0.9rem;">Treinos Completos</div>
                    <div style="font-size: 2.5rem; font-weight: 800; color: white;"><%= stats.completed || 0 %></div>
                </div>
                <div class="card-box">
                    <div style="color: #888; font-size: 0.9rem;">Sequ√™ncia (Dias)</div>
                    <div style="font-size: 2.5rem; font-weight: 800; color: var(--primary-color);"><%= stats.streak || 0 %> Ì¥•</div>
                </div>
                <div class="card-box" style="background: linear-gradient(135deg, #1a1a1a, #0d0d0d);">
                    <div style="color: #ccc; font-size: 0.9rem;">Pr√≥ximo Treino</div>
                    <h3 style="margin-top: 5px; color: white;">Superior A</h3>
                    <a href="/client/workouts" style="font-size: 0.8rem; color: var(--primary-color);">Iniciar agora &rarr;</a>
                </div>
            </div>

            <h3 class="mb-4">Hist√≥rico Recente</h3>
            <% if (workouts && workouts.length > 0) { %>
                <div style="display: flex; flex-direction: column; gap: 15px;">
                    <% workouts.forEach(w => { %>
                        <div class="card-box" style="display: flex; justify-content: space-between; align-items: center; padding: 15px;">
                            <div>
                                <strong style="color: white; display: block;"><%= w.title %></strong>
                                <span style="font-size: 0.8rem; color: #666;"><%= new Date(w.created_at).toLocaleDateString() %></span>
                            </div>
                            <span class="btn btn-sm btn-outline" style="font-size: 0.7rem;">Detalhes</span>
                        </div>
                    <% }) %>
                </div>
            <% } else { %>
                <p style="color: #666;">Nenhum treino registrado ainda.</p>
            <% } %>

        </main>
    </div>
    <script src="/js/main.js"></script>
</body>
</html>
