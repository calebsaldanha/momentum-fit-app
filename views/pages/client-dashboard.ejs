<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title><%= title %> | Momentum Fit</title>
    <link rel="icon" href="/images/momentum-fit-icon.png">
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
</head>
<body>

<div class="dashboard-layout">
    
    <%- include('../partials/sidebar.ejs') %>
    
    <div id="dashboardOverlay" class="dashboard-overlay"></div>

    <main class="app-main">
        <%- include('../partials/app-header.ejs') %>

        <div class="dashboard-content">
            <div style="margin-bottom: 30px;">
                <h1 style="color: white; font-size: 1.8rem; margin-bottom: 5px;">Olá, <span style="color: var(--primary);"><%= user.name.split(' ')[0] %></span>!</h1>
                <p style="color: #888; font-size: 0.95rem;">Resumo da sua jornada hoje.</p>
            </div>

            <div class="stats-grid">
                <div class="stat-card">
                    <div class="stat-icon"><i class="fas fa-dumbbell"></i></div>
                    <div>
                        <h3 style="margin:0; color:white; font-size: 1.5rem;"><%= stats.workoutsDone || 0 %></h3>
                        <p style="margin:0; color:#888; font-size:0.85rem;">Treinos</p>
                    </div>
                </div>
                <div class="stat-card">
                    <div class="stat-icon"><i class="fas fa-weight"></i></div>
                    <div>
                        <h3 style="margin:0; color:white; font-size: 1.5rem;"><%= stats.currentWeight || '--' %></h3>
                        <p style="margin:0; color:#888; font-size:0.85rem;">Peso (kg)</p>
                    </div>
                </div>
            </div>
            
            <div class="grid-2-col">
                <div class="feature-card">
                    <h3 style="font-size: 1.2rem; display: flex; align-items: center; gap: 10px;">
                        <i class="fas fa-play-circle" style="color: var(--primary);"></i> Treino Rápido
                    </h3>
                    <p style="margin-bottom: 20px;">Sem tempo? Gere um treino de 20min com IA.</p>
                    <a href="/client/ai-coach" class="btn btn-outline btn-sm text-center">Gerar Agora</a>
                </div>
            </div>

        </div>
    </main>
</div>

<script src="/js/main.js"></script>
</body>
</html>
