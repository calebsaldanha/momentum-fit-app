<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <%- include('../layouts/app/head') %>
    <title>Planos | Momentum Fit</title>
</head>
<body>
    <div class="dashboard-wrapper">
        <%- include('../partials/sidebar') %>
        
        <div class="main-content">
            <%- include('../partials/app-header', { title: 'Assinatura & Planos' }) %>

            <div class="content-scroll-area">
                
                <div style="text-align: center; margin-bottom: 40px;">
                    <h1 style="font-size: 2rem;">Invista na sua Evolução</h1>
                    <p style="color: #888;">Escolha o nível de suporte que sua ambição exige.</p>
                </div>

                <div class="grid-3">
                    <% 
                       // Normaliza o plano atual para comparação
                       const myPlan = user.plan ? user.plan.toLowerCase() : 'free';
                    %>

                    <div class="plan-card-option <%= myPlan === 'free' ? 'active-plan' : '' %>">
                        <% if(myPlan === 'free') { %>
                            <div style="position: absolute; top: 15px; right: 15px; background: var(--primary); color: black; font-size: 0.7rem; font-weight: 800; padding: 4px 8px; border-radius: 4px;">ATUAL</div>
                        <% } %>
                        <h3 style="color: white; font-size: 1.5rem;">Start</h3>
                        <div class="plan-price">R$ 0<span>/mês</span></div>
                        <ul class="check-list">
                            <li><i class="fas fa-check"></i> Acesso ao App</li>
                            <li><i class="fas fa-check"></i> Biblioteca Básica</li>
                            <li><i class="fas fa-check"></i> Rastreio de Peso</li>
                        </ul>
                        <div style="margin-top: auto;">
                            <% if(myPlan === 'free') { %>
                                <button class="btn btn-outline btn-block" disabled style="opacity: 0.5;">Plano Atual</button>
                            <% } else { %>
                                <a href="#" class="btn btn-outline btn-block">Downgrade</a>
                            <% } %>
                        </div>
                    </div>

                    <div class="plan-card-option <%= myPlan === 'pro' ? 'active-plan' : '' %>">
                        <% if(myPlan === 'pro') { %>
                            <div style="position: absolute; top: 15px; right: 15px; background: var(--primary); color: black; font-size: 0.7rem; font-weight: 800; padding: 4px 8px; border-radius: 4px;">ATUAL</div>
                        <% } else { %>
                            <div style="position: absolute; top: 0; right: 0; background: var(--primary); color: black; font-size: 0.7rem; font-weight: 800; padding: 5px 15px; border-bottom-left-radius: 10px;">POPULAR</div>
                        <% } %>
                        
                        <h3 style="color: var(--primary); font-size: 1.5rem;">Pro AI</h3>
                        <div class="plan-price">R$ 89<span>/mês</span></div>
                        <ul class="check-list">
                            <li><i class="fas fa-check-double"></i> <strong>Coach IA Ilimitado</strong></li>
                            <li><i class="fas fa-check"></i> Treinos Adaptativos</li>
                            <li><i class="fas fa-check"></i> Análise de Execução</li>
                        </ul>
                        <div style="margin-top: auto;">
                            <% if(myPlan === 'pro') { %>
                                <button class="btn btn-outline btn-block" disabled style="opacity: 0.5;">Plano Atual</button>
                            <% } else { %>
                                <a href="/client/checkout?plan=pro" class="btn btn-primary btn-block">Fazer Upgrade</a>
                            <% } %>
                        </div>
                    </div>

                    <div class="plan-card-option <%= myPlan === 'vip' ? 'active-plan' : '' %>">
                        <% if(myPlan === 'vip') { %>
                            <div style="position: absolute; top: 15px; right: 15px; background: var(--primary); color: black; font-size: 0.7rem; font-weight: 800; padding: 4px 8px; border-radius: 4px;">ATUAL</div>
                        <% } %>
                        <h3 style="color: white; font-size: 1.5rem;">Personal VIP</h3>
                        <div class="plan-price">R$ 249<span>/mês</span></div>
                        <p style="font-size: 0.8rem; color: #666; margin-bottom: 15px;">Vagas Limitadas</p>
                        <ul class="check-list">
                            <li><i class="fas fa-user"></i> Treinador Humano</li>
                            <li><i class="fas fa-comments"></i> WhatsApp Direto</li>
                            <li><i class="fas fa-video"></i> Videoconferência</li>
                        </ul>
                        <div style="margin-top: auto;">
                            <% if(myPlan === 'vip') { %>
                                <button class="btn btn-outline btn-block" disabled style="opacity: 0.5;">Plano Atual</button>
                            <% } else { %>
                                <button class="btn btn-outline btn-block" disabled>Lista de Espera</button>
                            <% } %>
                        </div>
                    </div>

                </div>
            </div>
        </div>
    </div>
    <script src="/js/layout.js"></script>
</body>
</html>
