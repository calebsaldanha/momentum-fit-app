<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <%- include('../partials/header') %>
    <link rel="stylesheet" href="/css/dashboard.css">
</head>
<body>
    <div class="dashboard-container">
        <%- include('../partials/sidebar', { user: user, currentPage: 'superadmin-dashboard' }) %>

        <main class="dashboard-main">
            <header class="page-header">
                <div>
                    <h1>Olá, <span class="text-neon"><%= user.name %></span></h1>
                    <p>Visão geral do desempenho da plataforma hoje.</p>
                </div>
                <div class="date-badge text-muted">
                    <i class="far fa-calendar"></i> <%= new Date().toLocaleDateString('pt-BR', { weekday: 'long', day: 'numeric', month: 'long' }) %>
                </div>
            </header>

            <div class="grid-stats">
                <div class="card-modern">
                    <div class="d-flex align-items-center gap-3">
                        <div style="padding: 12px; background: rgba(212, 255, 0, 0.1); border-radius: 12px; color: var(--primary);">
                            <i class="fas fa-users fa-lg"></i>
                        </div>
                        <div>
                            <p class="text-muted m-0 small">Total Usuários</p>
                            <h2 class="m-0"><%= stats.users %></h2>
                        </div>
                    </div>
                </div>
                <div class="card-modern">
                    <div class="d-flex align-items-center gap-3">
                        <div style="padding: 12px; background: rgba(16, 185, 129, 0.1); border-radius: 12px; color: var(--success);">
                            <i class="fas fa-dumbbell fa-lg"></i>
                        </div>
                        <div>
                            <p class="text-muted m-0 small">Treinadores</p>
                            <h2 class="m-0"><%= stats.trainers %></h2>
                        </div>
                    </div>
                </div>
                <div class="card-modern">
                    <div class="d-flex align-items-center gap-3">
                        <div style="padding: 12px; background: rgba(59, 130, 246, 0.1); border-radius: 12px; color: #3b82f6;">
                            <i class="fas fa-user-check fa-lg"></i>
                        </div>
                        <div>
                            <p class="text-muted m-0 small">Alunos Ativos</p>
                            <h2 class="m-0"><%= stats.clients %></h2>
                        </div>
                    </div>
                </div>
            </div>

            <h3 class="mb-3 fs-5">Acesso Rápido</h3>
            <div class="actions-grid">
                <a href="/superadmin/manage" class="action-card">
                    <i class="fas fa-users-cog"></i>
                    <span>Gerenciar Usuários</span>
                </a>
                <a href="/superadmin/create-trainer" class="action-card">
                    <i class="fas fa-user-plus"></i>
                    <span>Novo Treinador</span>
                </a>
                <a href="/articles/manage" class="action-card">
                    <i class="fas fa-newspaper"></i>
                    <span>Gerenciar Artigos</span>
                </a>
                <a href="/superadmin/settings" class="action-card">
                    <i class="fas fa-sliders-h"></i>
                    <span>Configurações</span>
                </a>
            </div>
        </main>
    </div>
</body>
</html>
