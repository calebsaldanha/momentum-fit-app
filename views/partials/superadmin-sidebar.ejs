<aside class="dashboard-sidebar">
    <div class="sidebar-header">
        <div class="user-avatar-large">
            <i class="fas fa-crown"></i>
        </div>
        <h3 class="user-name"><%= user.name.split(' ')[0] %></h3>
        <span class="user-role-badge">Gestão Global</span>
    </div>

    <nav class="sidebar-nav">
        <a href="/superadmin/dashboard" class="nav-item <%= currentPage === 'superadmin-dashboard' ? 'active' : '' %>">
            <i class="fas fa-chart-line"></i> <span>Dashboard Geral</span>
        </a>
        <a href="/superadmin/manage" class="nav-item <%= currentPage === 'superadmin-manage' ? 'active' : '' %>">
            <i class="fas fa-users-cog"></i> <span>Gerenciar Usuários</span>
        </a>
        <a href="/articles/manage" class="nav-item <%= currentPage === 'articles-manage' ? 'active' : '' %>">
            <i class="fas fa-newspaper"></i> <span>Gerenciar Blog</span>
        </a>

        <div style="margin-top: 15px; padding: 10px 24px;">
            <p style="font-size: 0.65rem; color: #666; margin-bottom: 8px; text-transform: uppercase; letter-spacing: 1px;">Acesso Rápido</p>
            <a href="/admin/dashboard" class="nav-item" style="border: 1px solid rgba(255,255,255,0.1); border-radius: 8px; justify-content: center; padding: 10px;">
                <span>Modo Treinador &rarr;</span>
            </a>
        </div>
    </nav>
    
    <div class="sidebar-footer">
         <form action="/auth/logout" method="POST" style="margin: 0;">
            <input type="hidden" name="_csrf" value="<%= typeof csrfToken !== 'undefined' ? csrfToken : '' %>">
            <button type="submit" class="btn-logout-sidebar">
                <i class="fas fa-sign-out-alt"></i> <span>Sair</span>
            </button>
        </form>
    </div>
</aside>
