<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= typeof title !== 'undefined' ? title : 'Momentum Fit' %></title>
    <link rel="stylesheet" href="/css/styles.css">
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <meta name="csrf-token" content="<%= typeof csrfToken !== 'undefined' ? csrfToken : '' %>">
</head>
<body>
    <header class="main-header">
        <div class="header-logo">
            <a href="/">
                <img src="/images/momentum-fit-logo-completo.png" alt="Momentum Fit" class="header-logo-img">
            </a>
        </div>
        
        <div class="user-actions">
            <% if (user) { %>
                <%- include("notification-bell.ejs") %>
                
                <div class="user-profile">
                    <div class="user-avatar-sm">
                        <%= user.name.charAt(0).toUpperCase() %>
                    </div>
                    <span class="user-name"><%= user.name %></span>
                </div>
                
                <form action="/auth/logout" method="POST" style="margin: 0;">
                    <input type="hidden" name="_csrf" value="<%= typeof csrfToken !== 'undefined' ? csrfToken : '' %>">
                    <button type="submit" class="logout-btn">Sair</button>
                </form>
            <% } else { %>
                <a href="/auth/login" class="btn-login">Entrar</a>
            <% } %>
        </div>
    </header>
    <script src="/js/main.js"></script>
    <input type="hidden" id="global-csrf-token" value="<%= typeof csrfToken !== 'undefined' ? csrfToken : '' %>">
