<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= typeof title !== 'undefined' ? title + ' | Momentum Fit' : 'Momentum Fit' %></title>
    <link rel="icon" type="image/png" href="/images/momentum-fit-icon.png">
    
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" href="/css/dashboard.css">
</head>
<body>
    <div class="app-container">
        <header class="main-header">
            <div class="header-content">
                <a href="/" class="brand-link">
                    <img src="/images/momentum-fit-logo-completo.png" alt="Momentum Fit" style="height: 32px;" onerror="this.style.display='none';this.nextElementSibling.style.display='block'">
                    <span style="display:none; font-weight:700; font-size:1.25rem; color:white;">Momentum Fit</span>
                </a>

                <nav class="nav-desktop desktop-only">
                    <% const isDashboard = typeof currentPage !== 'undefined'; %>

                    <% if (!isDashboard) { %>
                        <a href="/" class="nav-link">Início</a>
                        <a href="/about" class="nav-link">Sobre</a>
                        <a href="/articles" class="nav-link">Artigos</a>
                        <div class="nav-separator" style="width:1px; height:20px; background:var(--border); margin:0 1rem;"></div>
                    <% } %>

                    <% if (typeof user !== 'undefined' && user) { %>
                        <% 
                            let dashLink = '/client/dashboard';
                            if (user.role === 'superadmin') dashLink = '/superadmin/dashboard';
                            else if (user.role === 'trainer') dashLink = '/admin/dashboard';
                        %>
                        <% if (!isDashboard) { %>
                            <a href="<%= dashLink %>" class="btn btn-primary btn-sm">Painel</a>
                        <% } %>
                        <a href="/auth/logout" onclick="event.preventDefault(); document.getElementById('logout-form-header').submit();" class="nav-link logout-link">
                            Sair
                        </a>
                        <form id="logout-form-header" action="/auth/logout" method="POST" style="display: none;">
                            <input type="hidden" name="_csrf" value="<%= typeof csrfToken !== 'undefined' ? csrfToken : '' %>">
                        </form>
                    <% } else { %>
                        <a href="/auth/login" class="nav-link">Entrar</a>
                        <a href="/auth/register" class="btn btn-primary btn-sm">Começar Agora</a>
                    <% } %>
                </nav>

                <button id="mobileMenuBtn" class="mobile-menu-trigger mobile-only">
                    <i class="fas fa-bars"></i>
                </button>
            </div>

            <div id="mobileDrawer" class="mobile-drawer mobile-only">
                <div class="mobile-drawer-content">
                    
                    <% if (typeof user !== 'undefined' && user) { %>
                        <div style="display:flex; align-items:center; gap:10px; margin-bottom:10px; padding-bottom:15px; border-bottom:1px solid var(--border);">
                            <div style="width:36px; height:36px; background:var(--primary); color:black; border-radius:50%; display:flex; align-items:center; justify-content:center; font-weight:bold;">
                                <%= user.name.charAt(0).toUpperCase() %>
                            </div>
                            <div>
                                <div style="font-weight:600; color:white;"><%= user.name.split(' ')[0] %></div>
                                <div style="font-size:0.8rem; color:var(--text-muted);">
                                    <%= user.role === 'client' ? 'Aluno(a)' : 'Treinador' %>
                                </div>
                            </div>
                        </div>

                        <div class="mobile-public-links" style="border-bottom: 1px solid var(--border); padding-bottom: 10px; margin-bottom: 10px;">
                            <a href="/" class="mobile-link">Início</a>
                            <a href="/about" class="mobile-link">Sobre</a>
                            <a href="/articles" class="mobile-link">Artigos</a>
                        </div>

                        <% const current = typeof currentPage !== 'undefined' ? currentPage : ''; %>
                        <div class="mobile-section-title">Menu do Painel</div>

                        <% if (user.role === 'client') { %>
                            <a href="/client/dashboard" class="mobile-link <%= current === 'dashboard' ? 'active' : '' %>">
                                <i class="fas fa-chart-pie" style="width:20px;"></i> Visão Geral
                            </a>
                            <a href="/client/workouts" class="mobile-link <%= current === 'workouts' ? 'active' : '' %>">
                                <i class="fas fa-dumbbell" style="width:20px;"></i> Meus Treinos
                            </a>
                            <a href="/chat" class="mobile-link <%= current === 'chat' ? 'active' : '' %>">
                                <i class="fas fa-comments" style="width:20px;"></i> Chat com Coach
                            </a>
                            <a href="/client/profile" class="mobile-link <%= current === 'profile' ? 'active' : '' %>">
                                <i class="fas fa-user-circle" style="width:20px;"></i> Meu Perfil
                            </a>
                        <% } else if (user.role === 'trainer') { %>
                            <a href="/admin/dashboard" class="mobile-link <%= current === 'dashboard' ? 'active' : '' %>">
                                <i class="fas fa-home" style="width:20px;"></i> Dashboard
                            </a>
                            <a href="/admin/clients" class="mobile-link <%= current === 'clients' ? 'active' : '' %>">
                                <i class="fas fa-users" style="width:20px;"></i> Meus Alunos
                            </a>
                            <a href="/chat" class="mobile-link <%= current === 'chat' ? 'active' : '' %>">
                                <i class="fas fa-comments" style="width:20px;"></i> Mensagens
                            </a>
                            <a href="/admin/manage-articles" class="mobile-link <%= current === 'articles' ? 'active' : '' %>">
                                <i class="fas fa-newspaper" style="width:20px;"></i> Artigos
                            </a>
                        <% } %>

                        <div style="border-top:1px solid var(--border); margin-top:10px; padding-top:10px;">
                            <a href="/auth/logout" onclick="event.preventDefault(); document.getElementById('logout-form-header').submit();" class="mobile-link" style="color:#ef4444;">
                                <i class="fas fa-sign-out-alt" style="width:20px;"></i> Sair
                            </a>
                        </div>

                    <% } else { %>
                        <a href="/" class="mobile-link">Início</a>
                        <a href="/about" class="mobile-link">Sobre</a>
                        <a href="/articles" class="mobile-link">Artigos</a>
                        <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 10px; margin-top: 1.5rem;">
                            <a href="/auth/login" class="btn btn-outline btn-full">Entrar</a>
                            <a href="/auth/register" class="btn btn-primary btn-full" style="justify-content:center;">Cadastrar</a>
                        </div>
                    <% } %>
                </div>
            </div>
        </header>

        <div style="position: fixed; top: 90px; right: 20px; z-index: 1100; max-width: 400px; width: 90%;">
            <% if (typeof messages !== 'undefined' && messages.error) { %>
                <div class="alert alert-error">
                    <i class="fas fa-exclamation-circle"></i> <span><%= messages.error %></span>
                    <button onclick="this.parentElement.remove()" style="margin-left:auto; background:none; border:none; color:inherit; cursor:pointer;">&times;</button>
                </div>
            <% } %>
            <% if (typeof messages !== 'undefined' && messages.success) { %>
                <div class="alert" style="background: rgba(16, 185, 129, 0.15); color: #34d399; border: 1px solid rgba(16, 185, 129, 0.3);">
                    <i class="fas fa-check-circle"></i> <span><%= messages.success %></span>
                    <button onclick="this.parentElement.remove()" style="margin-left:auto; background:none; border:none; color:inherit; cursor:pointer;">&times;</button>
                </div>
            <% } %>
        </div>
