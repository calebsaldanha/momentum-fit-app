<aside class="dashboard-sidebar">
    <div class="sidebar-header">
        <div class="user-avatar-large">
            <i class="fas fa-user"></i>
        </div>
        <h3 class="user-name"><%= user.name.split(' ')[0] %></h3>
        <% if (typeof profile !== 'undefined' && profile.main_goal) { %>
            <span class="user-goal"><%= profile.main_goal %></span>
        <% } %>
    </div>
    
    <nav class="sidebar-nav">
        <a href="/client/dashboard" class="sidebar-link <%= typeof currentPage !== 'undefined' && currentPage === 'dashboard' ? 'active' : '' %>">
            <i class="fas fa-home"></i> <span>Visão Geral</span>
        </a>
        <a href="/client/workouts" class="sidebar-link <%= typeof currentPage !== 'undefined' && currentPage === 'workouts' ? 'active' : '' %>">
            <i class="fas fa-dumbbell"></i> <span>Meus Treinos</span>
        </a>
        <a href="/client/profile" class="sidebar-link <%= typeof currentPage !== 'undefined' && currentPage === 'profile' ? 'active' : '' %>">
            <i class="fas fa-user-cog"></i> <span>Meu Perfil</span>
        </a>
        </nav>
</aside>
